import XCTest
@testable import D4LocalAuth

final class D4LocalAuthTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(D4LocalAuth().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
